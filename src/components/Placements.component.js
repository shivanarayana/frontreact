import React, { Component } from 'react';
import { render } from 'react-dom';

export default class Placements extends Component {
render(){
    return(
        <div><h1>Placements page under construction</h1></div>
    );
}
}